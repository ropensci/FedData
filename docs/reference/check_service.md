# Check whether a web service is unavailable, and stop function if necessary.

Check whether a web service is unavailable, and stop function if
necessary.

## Usage

``` r
check_service(x)
```

## Arguments

- x:

  The path to the web service.

## Value

Error if service unavailable.
